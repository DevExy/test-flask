# Core dependencies
Flask==2.3.3
opencv-python==4.8.1.78
numpy==1.26.4
tensorflow==2.15.0
psutil==5.9.6

# Optional GPU monitoring (commented out as it's optional)
# GPUtil==1.4.0

# Additional dependencies that might be needed
Werkzeug==2.3.7  # Used by Flask for file uploads
Jinja2==3.1.2    # Flask templating engine